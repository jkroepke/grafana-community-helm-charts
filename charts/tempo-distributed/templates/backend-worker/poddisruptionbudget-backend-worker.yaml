{{- if and .Values.backendScheduler.enabled (gt (int .Values.backendWorker.replicas) 1) .Values.backendWorker.podDisruptionBudget.enabled }}
{{ $dict := dict "ctx" . "component" "backend-worker" "memberlist" true }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "tempo.resourceName" $dict }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tempo.labels" $dict | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "tempo.selectorLabels" $dict | nindent 6 }}
  maxUnavailable: {{ .Values.backendWorker.maxUnavailable }}
{{- end }}
