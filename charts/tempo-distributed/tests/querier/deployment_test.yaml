# $schema: https://raw.githubusercontent.com/helm-unittest/helm-unittest/refs/heads/main/schema/helm-testsuite.json
suite: querier Deployment
templates:
  - querier/deployment-querier.yaml
  - configmap-tempo.yaml

tests:
  - it: renders a Deployment with correct name and kind
    template: querier/deployment-querier.yaml
    asserts:
      - equal:
          path: kind
          value: Deployment
      - equal:
          path: metadata.name
          value: RELEASE-NAME-tempo-querier

  - it: renders standard ports on the querier container
    template: querier/deployment-querier.yaml
    asserts:
      - contains:
          path: spec.template.spec.containers[0].ports
          content:
            containerPort: 7946
            name: http-memberlist
            protocol: TCP
      - contains:
          path: spec.template.spec.containers[0].ports
          content:
            containerPort: 3200
            name: http-metrics
            protocol: TCP

  - it: does not render trace ports by default
    template: querier/deployment-querier.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[0].ports
          value:
            - containerPort: 7946
              name: http-memberlist
              protocol: TCP
            - containerPort: 3200
              name: http-metrics
              protocol: TCP

  - it: renders user-defined querier.extraPorts
    template: querier/deployment-querier.yaml
    set:
      querier.extraPorts:
        - containerPort: 9999
          name: custom-port
          protocol: TCP
    asserts:
      - contains:
          path: spec.template.spec.containers[0].ports
          content:
            containerPort: 9999
            name: custom-port
            protocol: TCP

  - it: uses tempo.podSecurityContext when no component override is set
    template: querier/deployment-querier.yaml
    asserts:
      - equal:
          path: spec.template.spec.securityContext.fsGroup
          value: 1000

  - it: component podSecurityContext overrides tempo.podSecurityContext
    template: querier/deployment-querier.yaml
    set:
      querier.podSecurityContext:
        fsGroup: 2000
    asserts:
      - equal:
          path: spec.template.spec.securityContext.fsGroup
          value: 2000

  - it: defaults.podSecurityContext is used when neither component nor tempo override is set
    template: querier/deployment-querier.yaml
    set:
      tempo.podSecurityContext: null
      defaults.podSecurityContext:
        fsGroup: 3000
    asserts:
      - equal:
          path: spec.template.spec.securityContext.fsGroup
          value: 3000

  - it: uses tempo.securityContext for container securityContext by default
    template: querier/deployment-querier.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[0].securityContext.runAsUser
          value: 1000

  - it: component containerSecurityContext overrides tempo.securityContext
    template: querier/deployment-querier.yaml
    set:
      querier.containerSecurityContext:
        runAsUser: 2000
    asserts:
      - equal:
          path: spec.template.spec.containers[0].securityContext.runAsUser
          value: 2000

  - it: defaults.nodeSelector is applied when component has no nodeSelector
    template: querier/deployment-querier.yaml
    set:
      defaults.nodeSelector:
        kubernetes.io/os: linux
    asserts:
      - equal:
          path: spec.template.spec.nodeSelector
          value:
            kubernetes.io/os: linux

  - it: component nodeSelector overrides defaults.nodeSelector
    template: querier/deployment-querier.yaml
    set:
      defaults.nodeSelector:
        kubernetes.io/os: linux
      querier.nodeSelector:
        kubernetes.io/arch: amd64
    asserts:
      - equal:
          path: spec.template.spec.nodeSelector
          value:
            kubernetes.io/arch: amd64

  - it: defaults.tolerations are applied when component has no tolerations
    template: querier/deployment-querier.yaml
    set:
      defaults.tolerations:
        - key: dedicated
          operator: Equal
          value: tempo
          effect: NoSchedule
    asserts:
      - contains:
          path: spec.template.spec.tolerations
          content:
            key: dedicated
            operator: Equal
            value: tempo
            effect: NoSchedule

  - it: defaults.resources are used when neither component nor tempo resources are set
    template: querier/deployment-querier.yaml
    set:
      defaults.resources.limits.memory: 256Mi
      defaults.resources.requests.cpu: 100m
    asserts:
      - equal:
          path: spec.template.spec.containers[0].resources.limits.memory
          value: 256Mi
      - equal:
          path: spec.template.spec.containers[0].resources.requests.cpu
          value: 100m

  - it: component resources override defaults.resources
    template: querier/deployment-querier.yaml
    set:
      defaults.resources.limits.memory: 256Mi
      querier.resources.limits.memory: 512Mi
    asserts:
      - equal:
          path: spec.template.spec.containers[0].resources.limits.memory
          value: 512Mi

  - it: defaults.podAnnotations are applied to pod
    template: querier/deployment-querier.yaml
    set:
      defaults.podAnnotations:
        team: platform
    asserts:
      - equal:
          path: spec.template.metadata.annotations["team"]
          value: platform

  - it: component podAnnotations override defaults.podAnnotations
    template: querier/deployment-querier.yaml
    set:
      defaults.podAnnotations:
        env: default
      querier.podAnnotations:
        env: production
    asserts:
      - equal:
          path: spec.template.metadata.annotations["env"]
          value: production

  - it: global.extraArgs are prepended to component extraArgs
    template: querier/deployment-querier.yaml
    set:
      global.extraArgs:
        - -config.expand-env=true
      querier.extraArgs:
        - -log.level=debug
    asserts:
      - contains:
          path: spec.template.spec.containers[0].args
          content: -config.expand-env=true
      - contains:
          path: spec.template.spec.containers[0].args
          content: -log.level=debug

  - it: global.priorityClassName is used when no component override
    template: querier/deployment-querier.yaml
    set:
      global.priorityClassName: high-priority
    asserts:
      - equal:
          path: spec.template.spec.priorityClassName
          value: high-priority

  - it: defaults.priorityClassName overrides global.priorityClassName
    template: querier/deployment-querier.yaml
    set:
      global.priorityClassName: high-priority
      defaults.priorityClassName: medium-priority
    asserts:
      - equal:
          path: spec.template.spec.priorityClassName
          value: medium-priority

  - it: component priorityClassName overrides defaults and global
    template: querier/deployment-querier.yaml
    set:
      global.priorityClassName: high-priority
      defaults.priorityClassName: medium-priority
      querier.priorityClassName: low-priority
    asserts:
      - equal:
          path: spec.template.spec.priorityClassName
          value: low-priority

  - it: autoscaling disabled uses static replicas
    template: querier/deployment-querier.yaml
    set:
      querier.autoscaling.enabled: false
      querier.replicas: 3
    asserts:
      - equal:
          path: spec.replicas
          value: 3

  - it: autoscaling enabled omits replicas from spec
    template: querier/deployment-querier.yaml
    set:
      querier.autoscaling.enabled: true
    asserts:
      - notExists:
          path: spec.replicas

  - it: volumes include config, runtime-config and querier store
    template: querier/deployment-querier.yaml
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: config
            configMap:
              name: RELEASE-NAME-tempo-config
              items:
                - key: "tempo.yaml"
                  path: "tempo.yaml"
      - contains:
          path: spec.template.spec.volumes
          content:
            name: runtime-config
            configMap:
              name: RELEASE-NAME-tempo-runtime
              items:
                - key: "overrides.yaml"
                  path: "overrides.yaml"
      - contains:
          path: spec.template.spec.volumes
          content:
            name: tempo-querier-store
            emptyDir: {}

  - it: container name is querier and args contain target and config flags
    template: querier/deployment-querier.yaml
    asserts:
      - equal:
          path: spec.template.spec.containers[0].name
          value: querier
      - contains:
          path: spec.template.spec.containers[0].args
          content: -target=querier
      - contains:
          path: spec.template.spec.containers[0].args
          content: -config.file=/conf/tempo.yaml
