{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if and $isDistributed .Values.ruler.enabled }}
{{- include "loki.rbac.sidecar" (dict "target" "ruler" "component" .Values.ruler "ctx" .) }}
{{- end }}
