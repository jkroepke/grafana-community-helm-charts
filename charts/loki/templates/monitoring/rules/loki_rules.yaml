{{- /*
Generated from 'loki-rules' group from https://github.com/grafana/loki.git
Do not change in-place! In order to change this file first read following link:
https://github.com/grafana-community/helm-charts/tree/main/charts/loki/hack
*/ -}}
{{- if and .Values.monitoring.rules.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "loki.resourceName" (dict "ctx" $ "component" "loki-rules") }}
  namespace: {{ .Values.monitoring.namespace | default (include "loki.namespace" $) }}
  labels:
    {{- include "loki.labels" $ | nindent 4 }}
    {{- with .Values.monitoring.rules.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.monitoring.rules.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  groups:
  - name: loki_rules
    rules:
    - expr: histogram_quantile(0.99, sum(rate(loki_request_duration_seconds_bucket[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}le, {{ $.Values.monitoring.appInstanceLabelName }}, job))
      record: cluster_job:loki_request_duration_seconds:99quantile
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: histogram_quantile(0.50, sum(rate(loki_request_duration_seconds_bucket[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}le, {{ $.Values.monitoring.appInstanceLabelName }}, job))
      record: cluster_job:loki_request_duration_seconds:50quantile
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_sum[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, job) / sum(rate(loki_request_duration_seconds_count[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, job)
      record: cluster_job:loki_request_duration_seconds:avg
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_bucket[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}le, {{ $.Values.monitoring.appInstanceLabelName }}, job)
      record: cluster_job:loki_request_duration_seconds_bucket:sum_rate
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_sum[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, job)
      record: cluster_job:loki_request_duration_seconds_sum:sum_rate
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_count[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, job)
      record: cluster_job:loki_request_duration_seconds_count:sum_rate
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: histogram_quantile(0.99, sum(rate(loki_request_duration_seconds_bucket[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}le, {{ $.Values.monitoring.appInstanceLabelName }}, job, route))
      record: cluster_job_route:loki_request_duration_seconds:99quantile
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: histogram_quantile(0.50, sum(rate(loki_request_duration_seconds_bucket[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}le, {{ $.Values.monitoring.appInstanceLabelName }}, job, route))
      record: cluster_job_route:loki_request_duration_seconds:50quantile
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_sum[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, job, route) / sum(rate(loki_request_duration_seconds_count[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, job, route)
      record: cluster_job_route:loki_request_duration_seconds:avg
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_bucket[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}le, {{ $.Values.monitoring.appInstanceLabelName }}, job, route)
      record: cluster_job_route:loki_request_duration_seconds_bucket:sum_rate
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_sum[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, job, route)
      record: cluster_job_route:loki_request_duration_seconds_sum:sum_rate
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_count[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, job, route)
      record: cluster_job_route:loki_request_duration_seconds_count:sum_rate
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: histogram_quantile(0.99, sum(rate(loki_request_duration_seconds_bucket[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}le, {{ $.Values.monitoring.appInstanceLabelName }}, namespace, job, route))
      record: cluster_namespace_job_route:loki_request_duration_seconds:99quantile
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: histogram_quantile(0.50, sum(rate(loki_request_duration_seconds_bucket[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}le, {{ $.Values.monitoring.appInstanceLabelName }}, namespace, job, route))
      record: cluster_namespace_job_route:loki_request_duration_seconds:50quantile
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_sum[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, namespace, job, route) / sum(rate(loki_request_duration_seconds_count[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, namespace, job, route)
      record: cluster_namespace_job_route:loki_request_duration_seconds:avg
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_bucket[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}le, {{ $.Values.monitoring.appInstanceLabelName }}, namespace, job, route)
      record: cluster_namespace_job_route:loki_request_duration_seconds_bucket:sum_rate
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_sum[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, namespace, job, route)
      record: cluster_namespace_job_route:loki_request_duration_seconds_sum:sum_rate
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
    - expr: sum(rate(loki_request_duration_seconds_count[1m])) by ({{ range $.Values.monitoring.rules.additionalAggregationLabels }}{{ . }},{{ end }}{{ $.Values.monitoring.appInstanceLabelName }}, namespace, job, route)
      record: cluster_namespace_job_route:loki_request_duration_seconds_count:sum_rate
        {{- if or (and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName) .Values.monitoring.rules.additionalRuleLabels }}
      labels:
        {{- if and .Values.monitoring.dashboards.multiCluster.enabled .Values.monitoring.dashboards.multiCluster.clusterName }}
        cluster: "{{ tpl .Values.monitoring.dashboards.multiCluster.clusterName $ }}"
        {{- end }}
        {{- with .Values.monitoring.rules.additionalRuleLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
{{- end }}